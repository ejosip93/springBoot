pipeline {
    environment {
        EMAIL_RECIPIENTS = 'josip.peric@decode.agency'
    }
    stages {
        stage('Build with unit testing') {
            echo 'Pulling...' + env.BRANCH_NAME
        }
    }
}
